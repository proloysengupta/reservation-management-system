
spring:
  application:
    name: hotel-service

    datasource:
      url: jdbc:mysql://mysql:3306/hoteldb
      #url: jdbc:mysql://localhost:3307/hoteldb
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8084
eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka/
      #defaultZone: http://localhost:8761/eureka/